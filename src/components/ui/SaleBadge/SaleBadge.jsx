import React from 'react';

const SaleBadge = () => {
    return (
        <span className="bg-red text-white text-xs font-bold px-2 py-1 uppercase">
            Sale
        </span>
    );
};

export default SaleBadge;